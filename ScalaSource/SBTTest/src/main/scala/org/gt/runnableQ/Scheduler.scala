package org.gt.runnableQ

/**
 * Created by gauravt on 10/02/15.
 */

/*trait Scheduler[T] {
  def execute(x : T)

  def addItem(x : T)

  def addItem(x : T, priority : Int) {
    require (priority > 0 && priority <= 10)
  }
}

object Scheduler {
  private class SchedulerImpl[T] extends Scheduler[T] {
    def execute(x : WorkItem) : Unit = {
      x.run()
    }

    def addItem(x : T) = {
      addItem(x, 5)
    }

    def addItem(x : T, priority : Int) = {

    }
  }
}*/
