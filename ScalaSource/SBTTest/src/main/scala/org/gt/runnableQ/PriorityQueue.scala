package org.gt.runnableQ

/**
 * Created by gauravt on 10/02/15.
 * A max heap implementation
 */

/*trait PriorityQueue[T] {
  //def enqueue(t : T, priority : Int) : PriorityQueue[T]

  //def enqueue(t : T, priority : Int) : PriorityQueue[T]
  def peek() : T

  //def dequeue() : (T, PriorityQueue[T])
}

object PriorityQueue {
  def apply[T](xs: T) = new PriorityQueueImpl(xs)

  private class PriorityQueueImpl[T](private val x : T) extends PriorityQueue[T] {

  }
}*/
